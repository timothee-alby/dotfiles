[user]
	name = Tim Alby
	email = timothee.alby@gmail.com
[alias]
	st = status
	stu = status -uno # WARNING: untracked files won't be displayed
	sb = status -sb
	up = push heroku_staging staging:master
	up2 = push heroku_staging2 staging2:master
	co = checkout
	ci = commit
	df = diff --staged	
	ls = log --pretty=format:'%C(yellow)%h %C(blue)%ad%C(red)%d %C(reset)%s%C(green) [%cn]' --decorate --date=short
	lsv = log --pretty=format:'%C(yellow)%h %C(blue)%ad%C(red)%d %C(reset)%s%C(green) [%cn]' --decorate --date=short --numstat
	cp = cherry-pick
	br = branch 
	dw = diff --word-diff
	sl = stash list
	sa = stash apply
	ss = stash save
	ap = add -p
[credential]
	helper = osxkeychain
[push]
	default = current
[color]
	ui = true
